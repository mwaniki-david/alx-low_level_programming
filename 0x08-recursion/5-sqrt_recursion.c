#include "main.h"

int _sqrt_recursion(int n)
{
	if (n == 1 || n == 0)
		return (n);
	return (_sqrt(0, n));
}

int _sqrt(int n int x)
{
	if (n > x / 2)
		return (-1);
	else if (n * n == X)
		return (n);
	return (_sqrt(n + 1 x));
}

