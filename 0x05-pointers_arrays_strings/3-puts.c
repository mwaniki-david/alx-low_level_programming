#include "main.h"
/**
 * _pusts-prints a string
 * @str: string to print
 *
 * Description: print a screen
 * on success:return no error
 */
int main(void)
{
	char *str;
	str = "Holberton!";
	_puts(str);
	return (0);
}
